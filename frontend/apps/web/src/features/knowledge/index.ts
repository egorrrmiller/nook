// Owner: frontend-knowledge — integration slots rendered by PageView / the shell (contracts §10).
// Signatures are fixed by the contract; implementations live in the sub-folders.
export { PageProperties } from './properties/PageProperties';
export { BacklinksPanel } from './backlinks/BacklinksPanel';
export { SearchDialog } from './search/SearchDialog';
export { ExportDialog } from './export/ExportDialog';
export { ImportDialog } from './import/ImportDialog';
export { TagChips } from './tags/TagChips';
